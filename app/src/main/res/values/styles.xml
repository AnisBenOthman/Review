<resources>

    <!-- Ton style progress bar premium -->
    <style name="Widget.App.Progress.Horizontal" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">10dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:progressTint">#FFB300</item>
        <item name="android:progressBackgroundTint">#E0E0E0</item>
        <item name="android:indeterminateOnly">false</item>
    </style>

</resources>
